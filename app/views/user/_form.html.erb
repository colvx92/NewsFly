<%= form_tag root_path, :method => :get do %>
  <%#= text_field_tag :source, nil, placeholder: "News Source", :class => "search" %>
  <%= button_tag "Search", :class => "button" %>

  <%= select_tag :source, options_for_select(@sources.map{|f| f }), class: 'form-control' %>

<% end %>
